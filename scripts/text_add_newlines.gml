var input_string = argument0;
var fit_to_width = argument1;

if (string_length(input_string) == 0) {
    return input_string;
}

if (string_width(input_string) > fit_to_width) {
    var space_position = string_length(input_string);
}
