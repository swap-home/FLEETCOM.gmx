if (sound_isplaying(ack1) || sound_isplaying(ack2) || sound_isplaying(ack3) ||
    sound_isplaying(ack4) || sound_isplaying(ack5) || sound_isplaying(ack6) ||
    sound_isplaying(ack7) || sound_isplaying(ack8) || sound_isplaying(ack9) ||
    sound_isplaying(ack10) || sound_isplaying(ack11) || sound_isplaying(ack12) ||
    sound_isplaying(ack13) || sound_isplaying(ack14) || sound_isplaying(ack15) ||
    sound_isplaying(ack16)) {
    
    return true;
} else {
    return false;
}
